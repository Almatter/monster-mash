import {pathToFileURL} from 'node:url';import assert from 'node:assert/strict';
const {chromium}=await import(process.env.PLAYWRIGHT_PATH?pathToFileURL(process.env.PLAYWRIGHT_PATH).href:'playwright');const browser=await chromium.launch({headless:true,...(process.env.BROWSER_PATH?{executablePath:process.env.BROWSER_PATH}:{})});const context=await browser.newContext({viewport:{width:844,height:390},hasTouch:true,isMobile:true,deviceScaleFactor:2});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4173');await page.locator('#name').fill("Azrael O'Night - 王");await page.locator('[data-monster=overlord]').tap();await page.locator('[data-channel=primary][data-color="#eee5d4"]').tap();await page.locator('[data-channel=power][data-color="#8862c7"]').tap();await page.screenshot({path:'test-results/event-mobile-selection.png',fullPage:true});const target=await page.locator('.swatch').first().boundingBox();assert.ok(target.width>=44&&target.height>=44);await page.locator('#recordsButton').tap();assert.equal(await page.locator('#records').isVisible(),true);assert.ok((await page.locator('#recordList').textContent()).includes('Slay 500'));await page.locator('#closeRecords').tap();await page.locator('#startForm button[type=submit]').tap();assert.equal(await page.locator('#hudName').textContent(),"Azrael O'Night - 王");
const box=await page.locator('#joystick').boundingBox(),cdp=await context.newCDPSession(page);await cdp.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[{x:box.x+box.width/2,y:box.y+box.height/2,id:1}]});await cdp.send('Input.dispatchTouchEvent',{type:'touchMove',touchPoints:[{x:box.x+box.width*.85,y:box.y+box.height/2,id:1}]});await page.waitForTimeout(500);await cdp.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});for(const button of await page.locator('.ability').all())await button.tap();await page.waitForTimeout(1200);await page.screenshot({path:'test-results/event-mobile-combat.png'});await page.locator('#pause').tap();await page.locator('#endRun').tap();await page.screenshot({path:'test-results/event-mobile-result.png',fullPage:true});await page.locator('#resultRecords').tap();await page.locator('#closeRecords').tap();await page.locator('#again').tap();await page.setViewportSize({width:390,height:844});assert.equal(await page.locator('#rotate').isVisible(),true);await page.setViewportSize({width:844,height:390});assert.equal(await page.locator('#paused').isVisible(),true);await page.locator('#endRun').tap();await page.locator('#back').tap();await page.setViewportSize({width:390,height:844});await page.screenshot({path:'test-results/event-portrait.png',fullPage:true});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);console.log('Mobile registry, naming, colors, records, touch combat, results, portrait and resize passed.');await browser.close();
