import {renderMusicLayer,MUSIC,THEMES} from '../src/procedural-music.ts';import {writeFile,mkdir} from 'node:fs/promises';
const theme=process.argv[2]||'menu';if(!Object.hasOwn(THEMES,theme))throw Error('Choose menu, sovereign, overlord, titan, devourer, or calamity');const stems=[];for(let i=0;i<7;i++)stems.push(await renderMusicLayer(i,MUSIC.sampleRate,theme));const weights=[.65,1,.95,.85,.9,.2,0],n=stems[0].length,out=Buffer.alloc(44+n*2);out.write('RIFF');out.writeUInt32LE(36+n*2,4);out.write('WAVEfmt ',8);out.writeUInt32LE(16,16);out.writeUInt16LE(1,20);out.writeUInt16LE(1,22);out.writeUInt32LE(MUSIC.sampleRate,24);out.writeUInt32LE(MUSIC.sampleRate*2,28);out.writeUInt16LE(2,32);out.writeUInt16LE(16,34);out.write('data',36);out.writeUInt32LE(n*2,40);let peak=0;for(let i=0;i<n;i++){const v=stems.reduce((sum,s,j)=>sum+s[i]*weights[j],0)*.75;peak=Math.max(peak,Math.abs(v));out.writeInt16LE(Math.round(Math.max(-1,Math.min(1,v))*32767),44+i*2);}await mkdir('test-results',{recursive:true});await writeFile('test-results/'+theme+'-music-preview.wav',out);console.log({seconds:n/MUSIC.sampleRate,peak,bytes:out.length});
